# Data Files

This folder may contain large `.rds` objects tracked with Git LFS.

Required setup before committing large files:

```bash
git lfs install
git lfs track "*.rds"
```

Then add and commit as usual.
